const typography = {
  pxToRem: (size) => `${size / 16}rem`,
  fontSize: 13,
  button: {
    fontWeight: 400,
    fontSize: 13,
    lineHeight: 1.5,
    textTransform: undefined,
  },
};

export default typography;
